# stm32-baremetal-from-scratch
A minimal STM32 bare-metal project built entirely from scratch without STM32CubeMX, HAL, or LL libraries. This project uses manually copied CMSIS core files and custom header definitions to access STM32 peripherals directly through memory-mapped registers.
